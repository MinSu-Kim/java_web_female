<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.yi.java_web_female.dao.TitleMapper">

	 <!--sql 문장  -->
	<!-- <sql id="selectSql">
		select code, name, t_grant as tGrant from title
	</sql>
	
	<sql id="whereSql">
		<where>
			<if test="studId != null">
				where code='T0001';
			</if>
		</where>
	</sql>  -->
	<!-- selectTitleByNo -->
	
	<select id="selectTitleByNo" parameterType="Title" resultType="Title">
		select code, name, t_grant as tGrant from title where code=#{code};
	</select>
</mapper>