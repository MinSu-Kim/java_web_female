<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.yi.java_web_female.dao.GradeMapper">

	<!-- selectGradeByNo -->
	<select id="selectGradeByNo" parameterType="Grade" resultType="Grade">
		select code, name, rate from grade
		where code=#{code}
	</select>

	<!-- selectGradeByAll -->
	<select id="selectGradeByAll" resultType="Grade">
		select code, name, rate from grade
	</select>

	<!-- insert -->
	<insert id="insertGrade" parameterType="Grade">
		insert into grade(code, name, rate)
		values(#{code},	#{name}, rate=#{rate})
	</insert>

	<!-- update -->
	 <update id="updateGrade" parameterType="Grade">
		update grade 
		set code=#{code}, name=#{name},	rate=#{rate}
		where code=#{code}
	</update>
	
	<!-- delete -->
	<delete id="deleteGrade" parameterType="Grade">
		delete from grade
		where code=#{code}
	</delete>
</mapper>