<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.yi.java_web_female.dao.CustomerMapper">
	<select id="selectCustomerByName" parameterType="Customer" resultType="Customer">
		select code, Id, passwd, Name, address, phone, dob, email, emp_code, license, grade_code, rent_cnt 
		from customer 
		where name = #{name}
	</select>
	
	<select id="selectCustomerByAll" resultType="Customer">
		select code, Id, passwd, Name, address, phone, dob, email, emp_code, license, grade_code, rent_cnt 
		from customer 
	</select>
</mapper>